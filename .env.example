# Kopier til .env og tilpas værdierne
PORT=8765
NODE_ENV=development
SESSION_SECRET=skift-denne-hemmelighed-til-noget-laengere
ADMIN_PASSWORD=xtreme2026

# Netlify sætter NETLIFY=true automatisk ved deploy.
# Sæt disse miljøvariabler i Netlify Dashboard → Site settings → Environment variables:
#   ADMIN_PASSWORD, SESSION_SECRET, NODE_ENV=production
